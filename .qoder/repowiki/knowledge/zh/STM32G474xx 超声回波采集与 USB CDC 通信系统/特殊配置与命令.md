使用 CMake Presets 选择工具链：`cmake --preset=release-gcc` 或 `cmake --preset=release-clang` 生成 Ninja/Meson 构建文件后执行 `cmake --build --preset=release-*`；也可用 `cmake -B build -S . -DCMAKE_TOOLCHAIN_FILE=cmake/gcc-arm-none-eabi.cmake` 手动指定。