使用 gcc 工具链需在 PATH 中放置 `arm-none-eabi-` 系列二进制；使用 clang 工具链需设置环境变量 `CLANG_GCC_CMSIS_COMPILER` 与 `GCC_TOOLCHAIN_ROOT`（HYBRID 模式），并通过 `-DSTARM_TOOLCHAIN_CONFIG=STARM_HYBRID|STARM_NEWLIB|STARM_PICOLIBC` 选择后端。链接器脚本必须位于 `${CMAKE_SOURCE_DIR}/STM32G474XX_FLASH.ld`。