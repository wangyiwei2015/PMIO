- 工具链文件统一以 `set(CMAKE_SYSTEM_NAME Generic)` + `CMAKE_SYSTEM_PROCESSOR arm` 开头，并用 `TOOLCHAIN_PREFIX` 变量集中管理编译器/链接器/objcopy 前缀。
- MCU 相关编译选项抽取到 `TARGET_FLAGS` 变量后复用至 `CMAKE_C_FLAGS` 与 `CMAKE_EXE_LINKER_FLAGS`，避免重复声明。
- 调试/发布优化等级通过 `CMAKE_C_FLAGS_DEBUG` / `CMAKE_C_FLAGS_RELEASE` 成对设置，且 C++ 标志直接复用 C 标志再追加 `-fno-rtti -fno-exceptions -fno-threadsafe-statics`。
- 链接阶段统一启用 `-Wl,-Map=${CMAKE_PROJECT_NAME}.map`、`-Wl,--gc-sections` 与 `--print-memory-usage`，并将 `.map` 加入 `ADDITIONAL_CLEAN_FILES` 以便 clean 清理。