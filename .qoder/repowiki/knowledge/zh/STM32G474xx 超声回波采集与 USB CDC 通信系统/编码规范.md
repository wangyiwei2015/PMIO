- 硬件初始化统一放在 Core/Src/stm32g4xx_hal_msp.c 的 *_MspInit 钩子中，避免在 main.c 内散落 HAL 调用。
- 外设中断服务函数仅做状态标志置位与回调派发，实际处理逻辑下沉到应用层回调，保持 ISR 短小。
- DMA 传输采用双缓冲环形缓冲区模式，HAL 回调中只交换 head/tail 指针，不拷贝数据。
- USB CDC 数据发送走非阻塞队列，满时丢弃旧数据而非阻塞，保证采样链路不受 PC 端速度影响。